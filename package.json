{
  "name": "smtp-bridge",
  "version": "1.0.0",
  "description": "SMTP relay that forwards emails via HTTP API (Mailgun, SendGrid, Postmark, Resend)",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "keywords": ["smtp", "relay", "mailgun", "sendgrid", "postmark", "resend", "railway", "ghost"],
  "license": "MIT",
  "dependencies": {
    "smtp-server": "^3.13.0",
    "mailparser": "^3.7.0"
  }
}
